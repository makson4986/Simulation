package org.makson.endings;

import org.makson.field.Field;
import org.makson.simulation.SimulationState;

public abstract class CheckEndSimulation {
    public abstract SimulationState checkEnd(Field field);
}
